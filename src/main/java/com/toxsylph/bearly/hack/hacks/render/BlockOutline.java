package com.toxsylph.bearly.hack.hacks.render;



public class BlockOutline {

}

/*
RayTraceResult lookingAt = Bearly.mc.objectMouseOver;
if (lookingAt != null && lookingAt.getType() == RayTraceResult.Type.BLOCK) {
	Vector3d pos = lookingAt.getHitVec();
	RenderUtilOverlay.drawText(event.getMatrixStack(), 20, "Target" + pos.toString());
}
*/
